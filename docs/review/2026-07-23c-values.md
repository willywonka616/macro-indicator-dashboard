# Current headline values, review round `2026-07-23c` — reviewable summary

> **This file is frozen as of the round below and is not edited in place.**
> Prior values file: `docs/review/2026-07-23b-values.md` (base commit
> `5d8826a`). Every review pass gets its own new file under
> `docs/review/` — check STATUS.md's "current review-round files" note
> (top of file) for whichever round is actually current when you're
> reading this.
>
> **Base commit:** `6613635` (HEAD of `claude/new-session-ldotj8` when this
> file was written — the `data: monthly refresh` commit produced by the
> live `update-data.yml` run documented in
> `docs/review/2026-07-23c-verification.md`)
> **Written:** 2026-07-23T15:40:00Z UTC

**What changed this round:** no shipped values changed. This is a
closure round: `TASKmspdparsing.md` tested one bounded hypothesis for
why MSPD's individual security rows sum to ~2.9x the table's own "Total
Marketable" total, found it didn't hold, and closed the investigation
per the task's own instruction rather than opening a new one.
`data/manual.json`'s two "— if rollover problems" rows gained an updated
`note` recording both eliminated hypotheses; nothing else in
`public/data.json` differs from round b except routine data-refresh
drift (asOf dates advancing with the underlying live series).

## What was tested, and the result

| Hypothesis | Round tested | Result |
|---|---|---|
| CUSIP reopening (a security's multiple issue-date rows double/triple-count) | b (`TASKborrowingneed.md`) | **Disconfirmed** — deduplicating by CUSIP left the sum unchanged |
| Aggregation-level duplication (detail + per-class subtotal + grand-total rows all summed) | c (`TASKmspdparsing.md`, this round) | **Disconfirmed** — only 2 non-detail rows exist per snapshot (a Federal Financing Bank line + the grand total), not a per-class subtotal tier; detail-only ratio (2.9073) essentially unchanged from the naive ratio (~2.9074) |

**MSPD parsing is now closed.** The real cause of the ~2.9x gap between
individual security rows and the table's own reported total remains
unexplained — recorded plainly as a closed, bounded investigation in
`data/manual.json` and STATUS.md §31, not left open for a future session
to feel obliged to keep chasing.

## Government gauge — unchanged from round b

| Row | Z | Book (Mar 2025) | Live |
|---|---|---|---|
| Current borrowing need | +2.4z | 39% of revenue | 34% (live, unchanged) |
| — if rollover problems | +2.5z | 239% of revenue | *(manual — both hypotheses now closed, see note)* |
| Projected borrowing need | +2.8z | 44% of revenue | 38% (live, unchanged) |
| — if rollover problems | +2.9z | 254% of revenue | *(manual — same closure)* |
| Debt in own currency | −2.0z | 100% (constant) | — |

Overall gov gauge score: **+2.4z, unchanged.**

## Reading this table

- This is the first round this session with no new live figure and no
  frontend change — a genuine negative result, reported as plainly as
  every positive one.
- See `docs/review/2026-07-23b-values.md` for the full borrowing-need
  construction and its own verification detail (still current, this
  round changed nothing there).
- See `docs/review/2026-07-23c-verification.md` for the live
  classification-field dump behind this round's disconfirmation.

# Current headline values, review round `2026-07-22f` — reviewable summary

> **This file is frozen as of the round below and is not edited in place.**
> Prior values file: `docs/review/2026-07-22e-values.md` (base commit
> `7de91cc`). Every review pass gets its own new file under
> `docs/review/` — check STATUS.md's "current review-round files" note
> (top of file) for whichever round is actually current when you're
> reading this.
>
> **Base commit:** `a73c4df` (HEAD of `claude/new-session-ldotj8` when this
> file was written — the `data: monthly refresh` commit produced by the
> live `update-data.yml` run documented in
> `docs/review/2026-07-22f-verification.md`)
> **Written:** 2026-07-22T16:10:00Z UTC

**What changed this round:** one row's `note` text (commentary only).
`value`/`display`/`tag`/`history` for "Interest rate to keep debt flat
(Dalio eq. #3)" are unchanged — this round found no bug in the
calculation, only a missing explanation.

**Claim status: VERIFIED** — extracted by parsing the actual committed
`public/data.json` at commit `a73c4df`, not from memory or a mock.

## Panel: "Government debt" — one row's commentary expanded

| label | display (unchanged) | tag | asOf | key |
|---|---|---|---|---|
| Interest rate to keep debt flat (Dalio eq. #3) | 4.2% | projection | FY2026 | `interest_rate_to_keep_debt_flat` |

**Before** (round d/e-era `note`):
> CBO 10-Year Budget Projections, February 2026 baseline (FY2025–FY2036,
> current law). Actual average effective rate on marketable debt held by
> the public (Treasury, live, 2026-06): 3.41% — gap vs. the rate required
> to keep debt flat: −0.8pt (debt burden trending down at the actual
> rate, all else equal).

**After** (this round):
> CBO 10-Year Budget Projections, February 2026 baseline (FY2025–FY2036,
> current law). Actual average effective rate on marketable debt held by
> the public (Treasury, live, 2026-06): 3.41% — gap vs. the rate required
> to keep debt flat: −0.8pt: the actual rate sits BELOW the stabilising
> threshold today — read carefully: not a settled cushion. The actual
> figure is an AVERAGE across all outstanding debt, including old
> low-coupon issues; new issuance costs more, so the average rate drifts
> up as the stock rolls over, closing this gap (or crossing it) over time
> even with no change in the required rate itself.

The old "debt burden trending down" framing was mechanically true as of
today's actual rate, but easy to misread as a stable, reassuring state.
The new text makes explicit that it isn't one.

## `equations.js` — two new caveats on the ƒx panel (not in `data.json`, static content)

```
"\"Revenue Growth\" is REVENUE growth, not GDP growth — matching Dalio's
own definition and the same basis the debt/revenue row uses, not GDP
(this matters under CBO's current-law baseline, where expiring tax
provisions are assumed to lapse and revenue is projected to grow faster
than GDP for a stretch of years)."

"A required-rate ABOVE the actual rate is easy to misread as reassuring.
It isn't a settled state: the actual figure is an average effective rate
across the WHOLE outstanding debt stock, including old low-coupon
issues — new issuance costs more, so that average drifts up as the stock
rolls over, closing (or crossing) this gap over time even with no change
in the required rate itself."
```

## Reading this table

- This round shipped no calculation change — `value`, `display`, `tag`,
  `asOf`, and `history` for the equation-#3 row are byte-identical to
  round e's. Only `note` (dynamic, per-run) and `equations.js`'s static
  caveats (frontend content, not part of `data.json` at all) changed.
- See `docs/review/2026-07-22e-values.md` for the full vitals/panels
  breakdown of every other row — unchanged this round.

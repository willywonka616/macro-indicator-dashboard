# Current headline values, review round `2026-07-22e` — reviewable summary

> **This file is frozen as of the round below and is not edited in place.**
> Prior values file: `docs/review/2026-07-22d-values.md` (base commit
> `174f680`). Every review pass gets its own new file under
> `docs/review/` — check STATUS.md's "current review-round files" note
> (top of file) for whichever round is actually current when you're
> reading this.
>
> **Base commit:** `7de91cc` (HEAD of `claude/new-session-ldotj8` when this
> file was written — the `data: monthly refresh` commit produced by the
> live `update-data.yml` run documented in
> `docs/review/2026-07-22e-verification.md`)
> **Written:** 2026-07-22T11:45:00Z UTC

**What changed this round: nothing in `public/data.json`.** This round
(`TASKtotaldebtreconcile.md`) added diagnostic-only prints to
`fetch.py --verify` and closed a calibration investigation in STATUS.md —
no shipped row's source, tag, or value changed. "Total debt (all
sectors)" continues to ship `TCMDO`, unchanged, at whatever value the
live fetch produces (362.6% this run).

**Claim status: VERIFIED** — confirmed by diffing this round's
`public/data.json` against round d's; no differences.

## The reconciliation table (diagnostic output, not a shipped row)

| Hypothesis | Result | vs. Dalio's 340% | Status |
|---|---|---|---|
| `TCMDO` (all sectors incl. financial) — **what ships** | 362.6% | +22.6pt | Closest of five, still doesn't match |
| Total debt minus government's own debt | 263.9% | −76.1pt | Eliminated (§14.4, prior round) |
| `TCMDODNS` (nonfinancial-only, excludes financial AND foreign) | 256.7% | −83.3pt | Eliminated (§14.4, prior round) |
| `TCMDO − DODFS` (isolated financial-sector-only, foreign still in) | 280.7% | −59.3pt | Eliminated (this round) |
| `TCMDO` at Dalio's own vintage (2025-Q1, same-quarter GDP) | 362.5% | +22.5pt | Eliminated (this round) — vintage changes nothing |

None of the four alternative hypotheses lands closer to 340% than `TCMDO`
itself. The gap (~23pts) is now a documented, closed known difference —
see `docs/review/2026-07-22e-verification.md` for the full investigation
and STATUS.md §26 for the permanent record.

## A note on the self-caught mistake

The first attempt at the vintage check divided 2025-Q1 debt by the
*current* GDP figure instead of 2025-Q1's own GDP, producing a
misleadingly-close-looking 341.8% for the wrong reason (a shrinking
denominator effect, not a real vintage match). This was caught and fixed
before being reported as a finding anywhere — both the wrong and
corrected figures are recorded in the verification log so the mistake and
its correction are both part of the reviewable trail, not just the final
answer.

## Reading this table

- This is the first round in this session where the "values" file
  reports **no change to any shipped row** — the deliverable is a closed
  investigation and a relabeled calibration row, not a data change. Per
  the task's own instruction: "the row keeps shipping — this is about
  labelling the calibration honestly, not about changing what the
  dashboard displays."
- See `docs/review/2026-07-22d-values.md` for the full vitals/panels
  breakdown of every row — unchanged this round.

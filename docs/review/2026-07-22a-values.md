# Current headline values, review round `2026-07-22a` — reviewable summary

> **This file is frozen as of the round below and is not edited in place.**
> Prior values file: `docs/review/2026-07-21d-values.md` (base commit
> `2a0a072`). Every review pass gets its own new file under
> `docs/review/` — check STATUS.md's "current review-round files" note
> (top of file) for whichever round is actually current when you're
> reading this.
>
> **Base commit:** `8a92e75` (HEAD of `claude/new-session-ldotj8` when this
> file was written — the `data: monthly refresh` commit produced by the
> live `update-data.yml` run documented in `docs/review/2026-07-22a-verification.md`,
> after the World Bank freshness-selector fix)
> **Written:** 2026-07-22T04:30:00Z UTC

**What changed this round:** one row in the "Liquid reserves" panel.
`data/manual.json`'s `goldPriceManualFallback` no longer fires under normal
operation. Every other vital and panel row is unchanged from round d.

**Claim status: VERIFIED** — extracted by parsing the actual committed
`public/data.json` at commit `8a92e75`, not from memory or a mock.

## Panel: "Liquid reserves" — one row changed

| label | value | display | unit | tone | tag (was) | asOf | key |
|---|---|---|---|---|---|---|---|
| Reserves incl. gold (market) | 4.9 | **4.9%** | of GDP | risk | **live** (was `manual_price`) | 2026-Q1 | `reserves_to_gdp` |
| FX reserves excl. gold | 0.8 | 0.8% | of GDP | risk | live (unchanged) | 2026-Q1 | `fx_reserves_excl_gold` |

**"Reserves incl. gold (market)"** — was `manual_price` as of the prior
round (§18-21): a live ounce count (Treasury) combined with a
hand-entered, dated gold price (`data/manual.json`'s
`goldPriceManualFallback`, $4,000/oz as of 2026-07-20), because the
previous live price source (DBnomics-mirrored IMF PCPS) had been frozen
since 2025-06. Now `tag: "live"` end to end — no manual price input:

```json
{
  "label": "Reserves incl. gold (market)",
  "value": 4.9,
  "display": "4.9%",
  "unit": "of GDP",
  "tone": "risk",
  "tag": "live",
  "src": "derived · Treasury (gold) + World Bank (Pink Sheet, direct), both 2026-06 + FRED",
  "asOf": "2026-Q1",
  "terms": [
    {"label": "Gold holdings (troy oz)", "src": "Treasury (fiscal_service, gold_reserve)", "asOf": "2026-06", "tag": "live"},
    {"label": "Gold price ($/oz)", "src": "World Bank (Pink Sheet, direct)", "asOf": "2026-06", "tag": "live"},
    {"label": "FX reserves excl. gold", "src": "FRED: TRESEGUSM052N", "asOf": "2026-03", "tag": "live"},
    {"label": "GDP", "src": "FRED: GDP", "asOf": "2026-Q1", "tag": "live"}
  ]
}
```

No `.note` field on this row this run (the `⚠`-prefixed staleness note
only appears when a fallback is actually active — see
`docs/review/2026-07-22a-verification.md` §5 for a screenshot of what it
looks like when one is).

Live gold price this run: **$4,228.00/oz** (2026-06, World Bank Commodity
Markets "Pink Sheet", direct download). Live gold holdings: Treasury's
`gold_reserve` series, 2026-06.

## `provenance.fallbacksFired` — no entry for gold this run

```json
[
  {"row": "World CB reserves in USD", "expectedTag": "live", "actualTag": "manual", "reason": "live IMF COFER fetch failed or exceeded its freshness threshold"}
]
```
(Pre-existing, unrelated COFER fallback — see STATUS.md §18. No entry for
"Reserves incl. gold (market)" — confirms the gold row shipped fully live,
not a partial/degraded state.)

## Reading this table

- The gold price is now sourced from the World Bank Commodity Markets
  ("Pink Sheet"), the same institutional data Dalio's own book implicitly
  relies on for commodity benchmarks, fetched two ways: a direct download
  from `thedocs.worldbank.org` (preferred when its own data is fresh) and
  a live-tested GitHub mirror (`datasets/gold-prices`) of the same
  underlying data as the fallback leg.
- `src` names which leg actually served the data this run
  ("World Bank (Pink Sheet, direct)" vs. "... GitHub mirror: ...") so a
  reader doesn't have to infer it — this mirrors the existing convention
  used for CBO's vintage label and every other multi-leg source in this
  project.
- See `docs/review/2026-07-21d-values.md` for the full vitals/panels
  breakdown of every other row — unchanged this round.
